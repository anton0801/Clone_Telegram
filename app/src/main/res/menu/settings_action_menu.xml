<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

<!--    чтобы иконки появильсь-->

    <item android:icon="@drawable/ic_btn_action_menu"
        app:showAsAction="ifRoom"
        android:title="">

        <menu>
            <item
                android:id="@+id/settings_menu_edit_name"
                android:icon="@drawable/ic_edit_btn_change_name"
                android:title="@string/settings_menu_change_name"
                android:orderInCategory="5"
                app:showAsAction="never" />

            <item
                android:id="@+id/settings_menu_exit"
                android:icon="@drawable/ic_btn_menu_exit"
                android:title="@string/settings_menu_exit"
                android:orderInCategory="8"
                app:showAsAction="never" />
        </menu>

    </item>

</menu>